/**
 * @file The primary entry point for the In-App Builder Obsidian plugin.
 *       This file is kept minimal, serving only to import and export the main plugin class.
 */

import { InAppBuilderPlugin } from './core/InAppBuilderPlugin';

// Export the main plugin class as the default export, as required by Obsidian.
export default InAppBuilderPlugin;
